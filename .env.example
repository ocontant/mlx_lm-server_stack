# MLX LM Server Environment Configuration

# Model Configuration
MLX_MODEL=mlx-community/Qwen2.5-Coder-32B-Instruct-8bit
MLX_PORT=11432
MLX_HOSTNAME=mlx.localhost.loc

# Model-specific settings
# Set to "true" to enable trust_remote_code (required for Qwen, Plamo, etc.)
TRUST_REMOTE_CODE=false
# Custom EOS token (e.g. "<|endoftext|>" for Qwen models)
EOS_TOKEN=
# Any additional command line arguments for mlx_lm.server
EXTRA_ARGS=""

# LiteLLM Configuration
LITELLM_PORT=11400
LITELLM_HTTP_HOSTNAME=litellm.localhost.loc

# Traefik Configuration
TRAEFIK_HOSTNAME=traefik.localhost.loc
TRAEFIK_DASHBOARD_PORT=8080

# HuggingFace Cache Directory
HF_CACHE_DIR=~/.cache/huggingface/hub/

# GitHub Repository Configuration for Docker Build
MLX_LM_REPO=https://github.com/ml-explore/mlx-lm.git
MLX_LM_BRANCH=main
# Optional additional repository to clone
EXTRA_REPO_URL=
EXTRA_REPO_BRANCH=main
EXTRA_REPO_DIR=/app/extra-repo

# SSL Configuration
# Set to "true" to enable automatic SSL certificate generation with Let's Encrypt
AUTO_SSL=false
# Certificate resolver to use (letsencrypt, letsencrypt-dns, etc.)
CERT_RESOLVER=letsencrypt
# Directory to store Let's Encrypt data
LETSENCRYPT_DATA_DIR=./traefik/letsencrypt